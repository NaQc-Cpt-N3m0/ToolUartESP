<Window x:Class="ToolUartESP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToolUartESP"
        mc:Ignorable="d"
        Title="ESP UART Communication Tool" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Connection Panel -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" 
                Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                CornerRadius="5" Margin="0,0,0,10" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="COM Port:" FontSize="14"/>
                            <ComboBox x:Name="comPortComboBox" Margin="0,0,0,10"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Margin="10,0,10,0">
                            <TextBlock Text="Baudrate:" FontSize="14"/>
                            <ComboBox x:Name="baudRateComboBox" Margin="0,0,0,10"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" Margin="10,0,0,0">
                            <TextBlock Text="Data Bits:" FontSize="14"/>
                            <ComboBox x:Name="dataSizeBox" Margin="0,0,0,10"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Status: " FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="connectionStatus" Text="Not Connected" Foreground="#e74c3c" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <Button x:Name="connectButton" Content="Connect" Padding="15,10"
                            Click="ConnectButton_Click" FontSize="14" Margin="10,0,0,0"/>
                    <Button x:Name="refreshPortsButton" Content="Refresh Ports" Padding="15,10"
                            Click="RefreshPortsButton_Click" FontSize="14" Margin="10,10,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Terminal Section -->
        <Border Grid.Row="1" Grid.Column="0" 
                Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                CornerRadius="5" Margin="0,10,10,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Received Data" FontWeight="Bold" FontSize="16" Grid.Row="0"/>
                
                <TextBox x:Name="uartReceiveTextBox" Grid.Row="1" Margin="0,10,0,0"
                         IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                         TextWrapping="Wrap" AcceptsReturn="True"
                         Background="#f9f9f9" FontFamily="Consolas"
                         BorderBrush="#DDDDDD" Padding="10"/>
            </Grid>
        </Border>
        
        <!-- Send Data Section -->
        <Border Grid.Row="1" Grid.Column="1" 
                Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                CornerRadius="5" Margin="10,10,0,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Send Data" FontWeight="Bold" FontSize="16" Grid.Row="0"/>
                
                <TextBox x:Name="uartSendTextBox" Grid.Row="1" Margin="0,10,0,10"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         Background="#f9f9f9" FontFamily="Consolas"
                         BorderBrush="#DDDDDD" Padding="10"/>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <CheckBox x:Name="appendCLRFCheckbox" Content="Append &lt;CL&gt;&lt;RF&gt;" 
                              VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button x:Name="sendButton" Content="Send" Width="100"
                            Click="SendButton_Click" HorizontalAlignment="Right"/>
                    <Button x:Name="clearSendButton" Content="Clear" Width="80"
                            Click="ClearSendButton_Click" Margin="10,0,0,0" 
                            Background="#95a5a6"/>
                    <Button x:Name="clearReceiveButton" Content="Clear Receive" Width="100"
                            Click="ClearReceiveButton_Click" Margin="10,0,0,0" 
                            Background="#95a5a6"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
